# Expo Video Minimal Example

Showcases how expo video does not work on Android in expo 53.0.8.

Run with
```bash
$ npx expo run:android --device
````

Then open `react devtools` with `j` in the runner instance.
